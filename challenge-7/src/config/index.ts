const options = {
	client: 'sqlite3',
	connection: {
		host: 'localhost',
		user: 'root',
		password: '',
		filename: './src/db/ecommerce.db',
		database: 'ecommerce',
	},
};

export default options;
