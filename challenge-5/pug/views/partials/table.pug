table.table
    thead
        tr
            th(scope='col') Name
            th(scope='col') Price
            th(scope='col') Photo URL
    tbody
        if data.length === 0
            tr
                td(colspan="3") There is no data
        else
            - for (let i = 0; i < data.length; ++i) {
                tr
                    td= data[i].name
                    td= data[i].price
                    td
                        img(src=data[i].url)
            - }